# vtop-feedback-extension
# this is an extension for vtop to fill an automated review of the  teachers

unpack this zip file amd load into the chrome browser using chrome developer tools inside load extension

then after signing in the vtop , click post then only  run this extension

